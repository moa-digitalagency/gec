# =================================================================
# GEC System Environment Variables Configuration
# Configuration des Variables d'Environnement du Système GEC
# =================================================================

# =================================================================
# DATABASE / BASE DE DONNÉES (Required/Requis - Provided by Replit)
# =================================================================
DATABASE_URL=postgresql://user:password@host:port/database

# =================================================================
# SECURITY / SÉCURITÉ (Required/Requis)
# =================================================================

# Flask Session Secret (Provided by Replit / Fourni par Replit)
SESSION_SECRET=your-super-secret-session-key-here-minimum-32-characters

# =================================================================
# ENCRYPTION KEYS / CLÉS DE CHIFFREMENT (CRITICAL/CRITIQUE)
# =================================================================
# WARNING: Generate these ONCE and keep them secure!
# ATTENTION: Générez ces clés UNE SEULE FOIS et gardez-les en sécurité!
#
# Generate with / Générer avec:
# python -c "import secrets, base64; print(base64.b64encode(secrets.token_bytes(32)).decode())"

# Master encryption key for sensitive data / Clé maître pour données sensibles
GEC_MASTER_KEY=

# Password salt for hashing / Sel pour hachage des mots de passe
GEC_PASSWORD_SALT=

# =================================================================
# ADMIN ACCESS / ACCÈS ADMINISTRATEUR (Optional/Optionnel)
# =================================================================

# Default admin password (sa.gec001)
# Mot de passe administrateur par défaut (sa.gec001)
# Default if not set / Par défaut: TempPassword123!
ADMIN_PASSWORD=YourSecurePassword123!

# =================================================================
# EMAIL CONFIGURATION / CONFIGURATION EMAIL (Optional/Optionnel)
# =================================================================

# SMTP Server / Serveur SMTP
# Examples / Exemples: smtp.gmail.com, smtp.office365.com, smtp.sendgrid.net
SMTP_SERVER=smtp.gmail.com

# SMTP Port / Port SMTP
# Common values / Valeurs courantes: 587 (TLS), 465 (SSL), 25 (unsecured/non sécurisé)
SMTP_PORT=587

# SMTP Sender Email / Email expéditeur SMTP
SMTP_EMAIL=notifications@yourdomain.com

# SMTP Password / Mot de passe SMTP
SMTP_PASSWORD=your-smtp-password

# Use TLS / Utiliser TLS (True or False / True ou False)
SMTP_USE_TLS=True

# =================================================================
# INSTRUCTIONS
# =================================================================
#
# English:
# 1. Copy this file to .env
# 2. Generate GEC_MASTER_KEY and GEC_PASSWORD_SALT (see commands above)
# 3. Fill in your specific values
# 4. NEVER commit .env to version control (it's in .gitignore)
# 5. Backup encryption keys in a secure location
#
# Français:
# 1. Copiez ce fichier vers .env
# 2. Générez GEC_MASTER_KEY et GEC_PASSWORD_SALT (voir commandes ci-dessus)
# 3. Remplissez vos valeurs spécifiques
# 4. NE JAMAIS commiter .env dans le contrôle de version (il est dans .gitignore)
# 5. Sauvegardez les clés de chiffrement dans un endroit sécurisé
# =================================================================
